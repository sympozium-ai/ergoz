apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: ergoz-agent
  labels:
    app.kubernetes.io/name: ergoz-agent
    {{- include "ergoz.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ergoz-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ergoz-agent
    spec:
      # Neither binary talks to the Kubernetes API — don't mount a token.
      automountServiceAccountToken: false
      {{- with .Values.agent.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: agent
          image: {{ include "ergoz.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: ERGOZ_SYSFS_ROOT
              value: /host/sys
            - name: ERGOZ_SAMPLE_INTERVAL
              value: {{ .Values.agent.sampleInterval | quote }}
            - name: ERGOZ_LISTEN
              value: ":{{ .Values.agent.port }}"
            {{- if .Values.nvidia.libHostDir }}
            - name: ERGOZ_NVML_PATH
              value: /host/nvidia/libnvidia-ml.so.1
            {{- end }}
          ports:
            - containerPort: {{ .Values.agent.port }}
              name: metrics
          volumeMounts:
            - name: sysfs
              mountPath: /host/sys
              readOnly: true
            {{- if .Values.nvidia.libHostDir }}
            - name: nvidia-lib
              mountPath: /host/nvidia
              readOnly: true
            {{- end }}
          securityContext:
            {{- include "ergoz.securityContext" . | nindent 12 }}
          resources:
            {{- toYaml .Values.agent.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: metrics
      volumes:
        - name: sysfs
          hostPath:
            path: /sys
            type: Directory
        {{- if .Values.nvidia.libHostDir }}
        - name: nvidia-lib
          hostPath:
            path: {{ .Values.nvidia.libHostDir }}
            type: Directory
        {{- end }}
